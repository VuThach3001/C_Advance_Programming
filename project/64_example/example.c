#include <stdio.h>

int main (void)
{
    int sum = 0, data[100] = {};
    while (i < 100)
        sum += data[i], ++i;

    int i = (5, 10); // 10 is assigned to i

    int j = (f1 (), f2 ());

    x = (y = 3, (z = ++y + 2) + 5);

    for (i = 0, j = 100; i != 10; ++i, j -= 10)
    {
        printf ("Thach");
        printf ("Jade");
        printf ("Done");
    }

    int houseprice = 249, 500;
    houseprice = 249;
    500;

    houseprice = (249, 500);

    char ch, date;
    int a = 1, b = 2;
    printf ("%d:%d", x, y);

    void (x, y);

    return 0;
}